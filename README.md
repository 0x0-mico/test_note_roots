Run `cargo run` to see the diff between note script procedures

Run `npm run dev` in `/web` folder to run the `web-sdk` app where you can see note details made with `web-sdk` and download them as serialized notes
